extends ../layout.pug 

block title 
    | Write Article

block page_title 
    | Write Article Freely

block content
    form(method="post")
        div
            input(type="text" id="title" name="title" placeholder='title')
        div 
            textarea(name="content" rows="10" cols="50") content 
        div 
            button(type="submit") Post